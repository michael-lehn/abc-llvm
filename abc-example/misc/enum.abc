#include <stdio.hdr>

enum Foo {
    A = 1,
    B,
    C = A + 5,
};

global i: int;

fn main(argc: int): int
{
    local a: Foo = C;

    printf("a = %d\n", a);

    local i: int = A + a;
    for (local i: int = 1; i < 5; ++i) {
	printf("i = %d\n", i);
    }

    return i;
}
