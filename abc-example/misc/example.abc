extern a: i32;
global a: i32;

extern fn foo(:i32): i32;

fn factorial(n: i32): i32
{
    if (n > 1) {
	return n * factorial(n -1);
    } else {
	return 1;
    }
}


enum Foo : int
{
    A = 1,
    B = A + 4,
    C,
};

fn main(argc: i32): i32
{
    local a: u16;
    a = A;

    enum Foo : int
    {
	A = 10,
	B = A + 4,
	C,
    };

    local b: i32;
    local c: i64;

    a += A;
    b = (u64)(i16)0xFFi8;

    c = a + b;

    // return foo(c + factorial(argc));
    return C;
}
