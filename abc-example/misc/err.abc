#include <assert.hdr>
#include <stdio.hdr>

enum Foo: i32 {
    FOO,
};

fn main(): int
{
    local p: -> int = nullptr;
    local q: array[10] of int;
    local f: const Foo;

    if (p == f) {
    }


    switch (f) {
	case FOO:
	    printf("FOO\n");
	    break;
    }
}
