extern a: i32;
global a: i32;

extern fn foo(:i32): i32;

fn factorial(n: i32): i32
{
    if (n > 1) {
	return n * factorial(n -1);
    } else {
	return 1;
    }
}

enum Foo : u8
{
    A = 0xFF,
    B = A + 4,
    C,
};

fn main(argc: i32): i32
{
    type Foo: i64;
    local a: Foo;
    a = A;

    return a;
}
