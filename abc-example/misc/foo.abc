#include <stdio.hdr>

global a: array[3] of int =  { 1, 2, 3};

fn main(argc: int, argv: ->-> char): int
{

    local b: -> array[3] of int;
    b = &a;

    for (local i: int = 0; i < 3; ++i) {
	printf("b[%d] = %d\n", i, b[i]);
    }
}
