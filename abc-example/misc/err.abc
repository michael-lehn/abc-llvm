#include <assert.hdr>
#include <stdio.hdr>

enum Foo: i32 {
    FOO,
};

fn main(): int
{
    local p: -> int = nullptr;
    local f: const Foo;

    assert(p);

    switch (f) {
	case FOO:
	    printf("FOO\n");
	    break;
    }
}
