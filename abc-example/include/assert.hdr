#ifndef ASSERT_HDR
#define ASSERT_HDR

#include <stdabc.hdr>

#define assert(EX) ((EX) || (__assert ("", __FILE__, __LINE__)))

extern fn __assert(msg: -> const char, file: -> const char, line: int): int;

#endif // ASSERT_HDR
